name: Windows Qt Build

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.8.3'
          arch: 'win64_mingw'

      - name: Configure
        run: cmake -S . -B build -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release

      - name: Deploy
        shell: cmd
        run: |
          if not exist dist mkdir dist
          copy build\cpp_assn5_2022.exe dist\cpp_assn5_2022.exe
          dir dist
          windeployqt dist\cpp_assn5_2022.exe

      - name: Zip
        shell: pwsh
        run: Compress-Archive -Path dist\* -DestinationPath cpp_assn5_2022-windows.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: cpp_assn5_2022-windows
          path: cpp_assn5_2022-windows.zip
